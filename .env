# Database Configuration for Vercel + Neon (PostgreSQL)
# Replace with your actual Neon database connection string
# Format: postgresql://username:password@host:port/database?sslmode=require
DATABASE_URL=postgresql://username:password@host:port/database?sslmode=require

# Session Configuration
SESSION_SECRET=your-session-secret-key-change-this-in-production

# API Keys (No longer required)
# GEMINI_API_KEY - Disabled, AI features removed

# Server Configuration (for local development)
PORT=3001
NODE_ENV=development

# PostgreSQL Configuration (Alternative to DATABASE_URL for local development)
# Use these for local PostgreSQL setup instead of Neon
DB_HOST=localhost
DB_USER=username
DB_PASSWORD=password
DB_NAME=jobtica_db
DB_PORT=5432

# Vercel Environment Variables (set these in Vercel dashboard)
# DATABASE_URL (Neon connection string)
# SESSION_SECRET
# GEMINI_API_KEY - No longer required, AI features disabled